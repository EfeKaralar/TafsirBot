import { useState } from "react";

const tafsirs = [
  {
    id: "ibn-kathir",
    name: "Ibn Kathir",
    arabic: "تفسير ابن كثير",
    full: "Tafsīr al-Qurʾān al-ʿAẓīm",
    era: "14th Century",
    author: "Ismāʿīl ibn Kathīr",
    method: "Maʾthūr (Tradition-based)",
    methodTag: "Narration",
    volume: "Long",
    english: true,
    priority: "Primary",
    color: "#c8a96e",
    tags: ["Hadith", "Sunni Orthodoxy", "Cross-referencing", "Isnad Chains"],
    audience: "General Sunni learners, hadith students",
    strengths: [
      "Interprets Quran with Quran, then Sunnah",
      "Rigorous hadith grading and criticism",
      "Rich cross-referencing between verses",
      "Most widely available in quality English translation",
    ],
    weaknesses: [
      "Repetitive due to isnad chains — must strip for RAG",
      "Occasionally accepts Isrāʾīliyyāt narrations",
      "Less focus on linguistic/grammatical nuance",
    ],
    ragNotes: {
      chunkSize: "Medium–Large",
      chunkStrategy: "Separate matn from isnad chains before embedding",
      metadataTags: ["surah", "ayah", "hadith_grade", "tafsir"],
      queryFit: "General Q&A, hadith context, cross-verse references",
      warning: "Isnad chains will pollute embedding space if not preprocessed",
    },
  },
  {
    id: "tabari",
    name: "Al-Ṭabarī",
    arabic: "تفسير الطبري",
    full: "Jāmiʿ al-Bayān ʿan Taʾwīl Āy al-Qurʾān",
    era: "10th Century",
    author: "Muḥammad ibn Jarīr al-Ṭabarī",
    method: "Maʾthūr (Encyclopedic)",
    methodTag: "Encyclopedic",
    volume: "Very Long",
    english: false,
    priority: "Secondary",
    color: "#8b6f47",
    tags: ["Scholarly", "All Opinions", "Linguistic", "Historical"],
    audience: "Researchers, scholars, comparative analysis",
    strengths: [
      "Unmatched breadth — collects all early opinions",
      "Preserves opinions not found elsewhere",
      "Strong Arabic linguistic and grammatical analysis",
      "Quasi-judicial: adjudicates between competing opinions",
    ],
    weaknesses: [
      "30+ volumes — enormous preprocessing challenge",
      "Contradictory opinions per verse require careful handling",
      "Requires scholarly expertise to navigate",
      "Limited quality English translation available",
    ],
    ragNotes: {
      chunkSize: "Large",
      chunkStrategy: "Preserve opinion markers ('some said X, others said Y'). Tag each opinion block separately.",
      metadataTags: ["surah", "ayah", "opinion_count", "scholar_cited"],
      queryFit: "Scholarly opinion retrieval, comparative analysis, historical views",
      warning: "Multi-opinion structure per verse needs special chunking logic",
    },
  },
  {
    id: "qurtubi",
    name: "Al-Qurṭubī",
    arabic: "تفسير القرطبي",
    full: "Al-Jāmiʿ li-Aḥkām al-Qurʾān",
    era: "13th Century",
    author: "Muḥammad ibn Aḥmad al-Qurṭubī",
    method: "Aḥkām (Legal-focused)",
    methodTag: "Legal / Fiqh",
    volume: "Long",
    english: false,
    priority: "Secondary",
    color: "#4a7c59",
    tags: ["Maliki", "Fiqh", "Comparative Law", "Ethics"],
    audience: "Students of Islamic law, fiqh comparativists",
    strengths: [
      "Best source for deriving legal rulings from Quran",
      "Compares all four madhabs on legal questions",
      "Covers social and ethical dimensions thoroughly",
      "Strong on verses related to worship, transactions, family law",
    ],
    weaknesses: [
      "Non-legal verses receive comparatively less depth",
      "Maliki bias in ambiguous rulings",
      "Limited English translation availability",
    ],
    ragNotes: {
      chunkSize: "Medium–Large",
      chunkStrategy: "Tag chunks with fiqh topics (salah, zakat, nikah, etc.). Legal reasoning blocks need to stay intact.",
      metadataTags: ["surah", "ayah", "fiqh_topic", "madhab_comparison"],
      queryFit: "Legal/fiqh queries, ruling derivation, madhab comparison",
      warning: "Route legal queries preferentially here via query classifier",
    },
  },
  {
    id: "jalalayn",
    name: "Al-Jalālayn",
    arabic: "تفسير الجلالين",
    full: "Tafsīr al-Jalālayn",
    era: "15th–16th Century",
    author: "Al-Maḥallī & Al-Suyūṭī",
    method: "Raʾy (Brief/Grammatical)",
    methodTag: "Concise",
    volume: "Short",
    english: true,
    priority: "Supporting",
    color: "#5b7fa6",
    tags: ["Grammar", "Lexical", "Brief", "Quick Reference"],
    audience: "Arabic students, quick lexical reference",
    strengths: [
      "Entire Quran in one slim volume — fast to process",
      "Excellent for Arabic grammar and basic semantic meaning",
      "High precision quick-gloss per verse phrase",
      "Good quality English translation (Feras Hamza)",
    ],
    weaknesses: [
      "No depth on hadith, fiqh, or historical context",
      "Extremely terse — insufficient as standalone source",
      "Some errors flagged by later scholars",
    ],
    ragNotes: {
      chunkSize: "Small",
      chunkStrategy: "Chunk per verse phrase. Use as a 'quick gloss' layer combined with deeper sources.",
      metadataTags: ["surah", "ayah", "phrase_segment"],
      queryFit: "Lexical/grammatical questions, quick meaning checks, Arabic word studies",
      warning: "Do not rely on this alone — always blend with a deeper tafsir for substantive queries",
    },
  },
  {
    id: "maududi",
    name: "Mawdūdī",
    arabic: "تفهيم القرآن",
    full: "Tafhīm al-Qurʾān",
    era: "20th Century",
    author: "Abū al-Aʿlā Mawdūdī",
    method: "Contextual / Thematic",
    methodTag: "Modern",
    volume: "Medium",
    english: true,
    priority: "Primary",
    color: "#9b6b9b",
    tags: ["Modern", "Accessible", "Socio-political", "Surah Context"],
    audience: "General modern readers, English-speaking audiences",
    strengths: [
      "Superb surah introductions with historical/thematic context",
      "Highly readable for English-speaking audiences",
      "Connects Quranic themes to contemporary life",
      "Self-contained paragraph commentary — very RAG-friendly",
    ],
    weaknesses: [
      "Ideological lens (Jamaat-e-Islami worldview) introduces bias",
      "Downplays classical scholarly disagreements",
      "Not a substitute for classical sources on rulings",
    ],
    ragNotes: {
      chunkSize: "Medium",
      chunkStrategy: "Surah introductions = their own chunk type. Verse commentary is paragraph-length and self-contained.",
      metadataTags: ["surah", "ayah", "chunk_type: intro|verse", "theme"],
      queryFit: "Modern relevance questions, surah context, thematic overviews",
      warning: "Flag ideological framing in outputs — add disclaimer for politically charged verses",
    },
  },
  {
    id: "ibn-ashur",
    name: "Ibn ʿĀshūr",
    arabic: "التحرير والتنوير",
    full: "Al-Taḥrīr wa-al-Tanwīr",
    era: "20th Century",
    author: "Muḥammad al-Ṭāhir ibn ʿĀshūr",
    method: "Linguistic / Maqāṣid-based",
    methodTag: "Academic",
    volume: "Long",
    english: false,
    priority: "Supporting",
    color: "#c05c3a",
    tags: ["Maqasid", "Linguistic", "Rationalist", "Reformist"],
    audience: "Advanced students, academics, reform-oriented scholarship",
    strengths: [
      "Extraordinary Arabic linguistic precision",
      "Maqāṣid al-Sharīʿah framework throughout",
      "Challenges weak narrations critically",
      "Engages with modernist and rationalist questions",
    ],
    weaknesses: [
      "Very demanding — requires high Arabic proficiency",
      "Limited English translation currently available",
      "Discursive essay-style; hard to chunk cleanly",
    ],
    ragNotes: {
      chunkSize: "Large",
      chunkStrategy: "Larger chunks needed to preserve argumentative flow. Avoid splitting mid-argument.",
      metadataTags: ["surah", "ayah", "maqasid_theme", "linguistic_focus"],
      queryFit: "Academic/linguistic queries, maqasid questions, Arabic word analysis",
      warning: "Primarily Arabic source — needs translation layer or multilingual embeddings",
    },
  },
];

const priorityColors = {
  Primary: { bg: "rgba(200, 169, 110, 0.15)", border: "#c8a96e", text: "#c8a96e" },
  Secondary: { bg: "rgba(139, 111, 71, 0.12)", border: "#8b8b6f", text: "#a0a07a" },
  Supporting: { bg: "rgba(90, 90, 90, 0.1)", border: "#666", text: "#999" },
};

export default function TafsirDashboard() {
  const [selected, setSelected] = useState(tafsirs[0]);
  const [activeTab, setActiveTab] = useState("overview");

  return (
    <div style={{
      fontFamily: "'Georgia', 'Times New Roman', serif",
      background: "#0d0d0d",
      minHeight: "100vh",
      color: "#e8e0d4",
      display: "flex",
      flexDirection: "column",
    }}>
      {/* Header */}
      <header style={{
        borderBottom: "1px solid #2a2a2a",
        padding: "28px 40px 20px",
        background: "linear-gradient(180deg, #111 0%, #0d0d0d 100%)",
      }}>
        <div style={{ maxWidth: 1100, margin: "0 auto" }}>
          <div style={{ display: "flex", alignItems: "baseline", gap: 16, marginBottom: 6 }}>
            <span style={{ fontSize: 11, letterSpacing: "0.2em", color: "#666", textTransform: "uppercase", fontFamily: "monospace" }}>
              AI Tafsir Bot · Corpus Selection
            </span>
          </div>
          <h1 style={{ margin: 0, fontSize: 28, fontWeight: 400, color: "#e8e0d4", letterSpacing: "0.01em" }}>
            Tafsir Reference Corpus
          </h1>
          <p style={{ margin: "6px 0 0", fontSize: 13, color: "#666", fontFamily: "monospace" }}>
            6 scholars · 3 priority tiers · RAG pipeline specifications
          </p>
        </div>
      </header>

      <div style={{ display: "flex", flex: 1, maxWidth: 1100, margin: "0 auto", width: "100%", padding: "0 40px" }}>
        {/* Sidebar */}
        <nav style={{
          width: 220,
          paddingTop: 28,
          paddingRight: 20,
          borderRight: "1px solid #1e1e1e",
          flexShrink: 0,
        }}>
          {/* Priority legend */}
          <div style={{ marginBottom: 24 }}>
            {["Primary", "Secondary", "Supporting"].map(p => (
              <div key={p} style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 6 }}>
                <div style={{
                  width: 8, height: 8, borderRadius: "50%",
                  background: priorityColors[p].border,
                }} />
                <span style={{ fontSize: 10, color: "#555", letterSpacing: "0.12em", textTransform: "uppercase", fontFamily: "monospace" }}>{p}</span>
              </div>
            ))}
          </div>

          {tafsirs.map(t => (
            <button
              key={t.id}
              onClick={() => setSelected(t)}
              style={{
                display: "block",
                width: "100%",
                textAlign: "left",
                background: selected.id === t.id ? "rgba(255,255,255,0.04)" : "transparent",
                border: "none",
                borderLeft: `2px solid ${selected.id === t.id ? t.color : "transparent"}`,
                padding: "10px 14px",
                cursor: "pointer",
                marginBottom: 2,
                transition: "all 0.15s",
              }}
            >
              <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
                <span style={{
                  fontSize: 13,
                  color: selected.id === t.id ? "#e8e0d4" : "#888",
                  fontWeight: selected.id === t.id ? 500 : 400,
                }}>{t.name}</span>
                <span style={{
                  fontSize: 9,
                  padding: "1px 6px",
                  borderRadius: 2,
                  background: priorityColors[t.priority].bg,
                  color: priorityColors[t.priority].text,
                  border: `1px solid ${priorityColors[t.priority].border}20`,
                  fontFamily: "monospace",
                  letterSpacing: "0.05em",
                }}>
                  {t.priority[0]}
                </span>
              </div>
              <div style={{ fontSize: 10, color: "#444", marginTop: 2, fontFamily: "monospace" }}>{t.era}</div>
            </button>
          ))}
        </nav>

        {/* Main content */}
        <main style={{ flex: 1, paddingTop: 28, paddingLeft: 32 }}>
          {/* Tafsir header */}
          <div style={{
            borderBottom: "1px solid #1e1e1e",
            paddingBottom: 20,
            marginBottom: 24,
          }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}>
              <div>
                <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 4 }}>
                  <h2 style={{ margin: 0, fontSize: 22, fontWeight: 400, color: selected.color }}>{selected.name}</h2>
                  <span style={{
                    fontSize: 10,
                    padding: "2px 8px",
                    borderRadius: 3,
                    background: priorityColors[selected.priority].bg,
                    color: priorityColors[selected.priority].text,
                    border: `1px solid ${priorityColors[selected.priority].border}30`,
                    fontFamily: "monospace",
                    letterSpacing: "0.1em",
                    textTransform: "uppercase",
                  }}>{selected.priority}</span>
                </div>
                <div style={{ fontSize: 18, color: "#555", fontFamily: "serif", marginBottom: 4, direction: "rtl", textAlign: "left" }}>
                  {selected.arabic}
                </div>
                <div style={{ fontSize: 12, color: "#555", fontStyle: "italic" }}>{selected.full}</div>
              </div>
              <div style={{ textAlign: "right" }}>
                <div style={{
                  display: "inline-block",
                  padding: "4px 12px",
                  border: `1px solid ${selected.color}40`,
                  borderRadius: 3,
                  fontSize: 11,
                  color: selected.color,
                  fontFamily: "monospace",
                  marginBottom: 6,
                }}>{selected.methodTag}</div>
                <div style={{ fontSize: 11, color: "#444", fontFamily: "monospace" }}>{selected.era}</div>
                <div style={{ fontSize: 11, color: "#444", fontFamily: "monospace" }}>
                  English: {selected.english ? "✓ Available" : "✗ Limited"}
                </div>
              </div>
            </div>

            {/* Tags */}
            <div style={{ display: "flex", gap: 6, flexWrap: "wrap", marginTop: 14 }}>
              {selected.tags.map(tag => (
                <span key={tag} style={{
                  fontSize: 10,
                  padding: "2px 8px",
                  background: "#1a1a1a",
                  border: "1px solid #2a2a2a",
                  borderRadius: 2,
                  color: "#666",
                  fontFamily: "monospace",
                  letterSpacing: "0.05em",
                }}>{tag}</span>
              ))}
            </div>
          </div>

          {/* Tabs */}
          <div style={{ display: "flex", gap: 0, marginBottom: 24, borderBottom: "1px solid #1e1e1e" }}>
            {[["overview", "Overview"], ["rag", "RAG Specs"], ["compare", "All Tafsirs"]].map(([id, label]) => (
              <button
                key={id}
                onClick={() => setActiveTab(id)}
                style={{
                  background: "transparent",
                  border: "none",
                  borderBottom: `2px solid ${activeTab === id ? selected.color : "transparent"}`,
                  padding: "8px 18px",
                  cursor: "pointer",
                  fontSize: 12,
                  color: activeTab === id ? "#e8e0d4" : "#555",
                  letterSpacing: "0.08em",
                  textTransform: "uppercase",
                  fontFamily: "monospace",
                  marginBottom: -1,
                  transition: "all 0.15s",
                }}
              >{label}</button>
            ))}
          </div>

          {/* Tab: Overview */}
          {activeTab === "overview" && (
            <div>
              <div style={{ marginBottom: 8, fontSize: 10, color: "#444", fontFamily: "monospace", textTransform: "uppercase", letterSpacing: "0.15em" }}>
                Author
              </div>
              <div style={{ fontSize: 14, color: "#bbb", marginBottom: 24 }}>{selected.author}</div>

              <div style={{ marginBottom: 8, fontSize: 10, color: "#444", fontFamily: "monospace", textTransform: "uppercase", letterSpacing: "0.15em" }}>
                Target Audience
              </div>
              <div style={{ fontSize: 14, color: "#bbb", marginBottom: 24 }}>{selected.audience}</div>

              <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 20 }}>
                <div>
                  <div style={{ marginBottom: 12, fontSize: 10, color: "#444", fontFamily: "monospace", textTransform: "uppercase", letterSpacing: "0.15em" }}>
                    Strengths
                  </div>
                  {selected.strengths.map((s, i) => (
                    <div key={i} style={{
                      display: "flex", gap: 10, marginBottom: 10, alignItems: "flex-start"
                    }}>
                      <span style={{ color: selected.color, fontSize: 14, lineHeight: 1.4, flexShrink: 0 }}>+</span>
                      <span style={{ fontSize: 13, color: "#999", lineHeight: 1.5 }}>{s}</span>
                    </div>
                  ))}
                </div>
                <div>
                  <div style={{ marginBottom: 12, fontSize: 10, color: "#444", fontFamily: "monospace", textTransform: "uppercase", letterSpacing: "0.15em" }}>
                    Weaknesses
                  </div>
                  {selected.weaknesses.map((w, i) => (
                    <div key={i} style={{
                      display: "flex", gap: 10, marginBottom: 10, alignItems: "flex-start"
                    }}>
                      <span style={{ color: "#664444", fontSize: 14, lineHeight: 1.4, flexShrink: 0 }}>−</span>
                      <span style={{ fontSize: 13, color: "#999", lineHeight: 1.5 }}>{w}</span>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          )}

          {/* Tab: RAG Specs */}
          {activeTab === "rag" && (
            <div>
              {[
                ["Chunk Size", selected.ragNotes.chunkSize],
                ["Chunking Strategy", selected.ragNotes.chunkStrategy],
                ["Query Fit", selected.ragNotes.queryFit],
              ].map(([label, value]) => (
                <div key={label} style={{
                  marginBottom: 20,
                  padding: "16px 18px",
                  background: "#111",
                  border: "1px solid #1e1e1e",
                  borderRadius: 4,
                }}>
                  <div style={{ fontSize: 9, color: "#444", fontFamily: "monospace", textTransform: "uppercase", letterSpacing: "0.2em", marginBottom: 8 }}>{label}</div>
                  <div style={{ fontSize: 13, color: "#bbb", lineHeight: 1.6 }}>{value}</div>
                </div>
              ))}

              <div style={{
                marginBottom: 20,
                padding: "16px 18px",
                background: "#111",
                border: "1px solid #1e1e1e",
                borderRadius: 4,
              }}>
                <div style={{ fontSize: 9, color: "#444", fontFamily: "monospace", textTransform: "uppercase", letterSpacing: "0.2em", marginBottom: 8 }}>Metadata Tags</div>
                <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
                  {selected.ragNotes.metadataTags.map(tag => (
                    <code key={tag} style={{
                      fontSize: 11,
                      padding: "3px 8px",
                      background: "#1a1a1a",
                      border: `1px solid ${selected.color}30`,
                      borderRadius: 3,
                      color: selected.color,
                    }}>{tag}</code>
                  ))}
                </div>
              </div>

              <div style={{
                padding: "14px 18px",
                background: "rgba(120, 60, 30, 0.1)",
                border: "1px solid #5a2a1a",
                borderRadius: 4,
                display: "flex",
                gap: 12,
                alignItems: "flex-start",
              }}>
                <span style={{ color: "#c05c3a", fontSize: 16, flexShrink: 0 }}>⚠</span>
                <div>
                  <div style={{ fontSize: 9, color: "#c05c3a", fontFamily: "monospace", textTransform: "uppercase", letterSpacing: "0.2em", marginBottom: 6 }}>Pipeline Warning</div>
                  <div style={{ fontSize: 13, color: "#aa7a6a", lineHeight: 1.6 }}>{selected.ragNotes.warning}</div>
                </div>
              </div>
            </div>
          )}

          {/* Tab: Compare all */}
          {activeTab === "compare" && (
            <div style={{ overflowX: "auto" }}>
              <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12 }}>
                <thead>
                  <tr>
                    {["Scholar", "Era", "Method", "Volume", "English", "Priority", "Best For"].map(h => (
                      <th key={h} style={{
                        textAlign: "left",
                        padding: "8px 12px",
                        borderBottom: "1px solid #2a2a2a",
                        fontSize: 9,
                        color: "#444",
                        fontFamily: "monospace",
                        textTransform: "uppercase",
                        letterSpacing: "0.15em",
                        whiteSpace: "nowrap",
                      }}>{h}</th>
                    ))}
                  </tr>
                </thead>
                <tbody>
                  {tafsirs.map(t => (
                    <tr
                      key={t.id}
                      onClick={() => { setSelected(t); setActiveTab("overview"); }}
                      style={{
                        cursor: "pointer",
                        background: selected.id === t.id ? "rgba(255,255,255,0.02)" : "transparent",
                        transition: "background 0.1s",
                      }}
                    >
                      <td style={{ padding: "10px 12px", borderBottom: "1px solid #1a1a1a" }}>
                        <span style={{ color: t.color, fontWeight: 500 }}>{t.name}</span>
                      </td>
                      <td style={{ padding: "10px 12px", borderBottom: "1px solid #1a1a1a", color: "#666", fontFamily: "monospace" }}>{t.era.split(" ")[0]}</td>
                      <td style={{ padding: "10px 12px", borderBottom: "1px solid #1a1a1a", color: "#888" }}>{t.methodTag}</td>
                      <td style={{ padding: "10px 12px", borderBottom: "1px solid #1a1a1a", color: "#666", fontFamily: "monospace" }}>{t.volume}</td>
                      <td style={{ padding: "10px 12px", borderBottom: "1px solid #1a1a1a", color: t.english ? "#4a7c59" : "#664444", fontFamily: "monospace" }}>
                        {t.english ? "✓" : "✗"}
                      </td>
                      <td style={{ padding: "10px 12px", borderBottom: "1px solid #1a1a1a" }}>
                        <span style={{
                          fontSize: 9,
                          padding: "2px 7px",
                          borderRadius: 2,
                          background: priorityColors[t.priority].bg,
                          color: priorityColors[t.priority].text,
                          fontFamily: "monospace",
                          textTransform: "uppercase",
                          letterSpacing: "0.05em",
                        }}>{t.priority}</span>
                      </td>
                      <td style={{ padding: "10px 12px", borderBottom: "1px solid #1a1a1a", color: "#666", fontSize: 11 }}>
                        {t.ragNotes.queryFit.split(",")[0]}
                      </td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          )}
        </main>
      </div>

      {/* Footer */}
      <footer style={{
        borderTop: "1px solid #1a1a1a",
        padding: "14px 40px",
        marginTop: 40,
      }}>
        <div style={{ maxWidth: 1100, margin: "0 auto", display: "flex", justifyContent: "space-between", alignItems: "center" }}>
          <span style={{ fontSize: 10, color: "#333", fontFamily: "monospace" }}>AI Tafsir Bot · Corpus Planning Document</span>
          <span style={{ fontSize: 10, color: "#333", fontFamily: "monospace" }}>6 tafsirs · 2 Primary · 2 Secondary · 2 Supporting</span>
        </div>
      </footer>
    </div>
  );
}
